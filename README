geo
---

Small and simple library for geo location tasks.

Currently includes a representation of a location and the possibility to measure distances between locations.
The distance calculation algorithms are pluggable. Currently, only haversine is implemented.
