geo
---
Small and simple library in the form of an OSGi bundle for geo location tasks.

Currently includes a representation of a location and the possibility to measure distances between locations.
The distance calculation algorithms are pluggable.

